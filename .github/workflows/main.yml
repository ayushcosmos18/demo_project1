name: Basic SH Pipeline

on: push

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Script
        run: sh script.sh
